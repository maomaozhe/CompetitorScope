# PRD - CompetitorScope

> 产品需求文档 - 记录待实现的功能和优化项
> 最后更新：2026-05-14

---

## 待开发

### P0（Demo 前必须解决）

- [ ] **流式输出**：每个 Agent 节点运行时实时渲染中间态，Writer 生成报告时渐进式输出，不是黑盒一次性生成
- [ ] **HITL 倒计时修复**：倒计时正常倒数到 0，不反复横跳

### P1（Demo 后）

- [ ] 报告引用质量优化（每个结论有证据，引用准确）
- [ ] DB 持久化完善（当前运行态主要靠 RUN_STORE）
- [ ] 错误处理边界场景（断网、LLM 限流、数据完全抓不到）

### P2（未来）

- [ ] 预录 trace 回放兜底
- [ ] CLI 完整版（`ca run`/`status`/`list`/`report`/`evidence`）
- [ ] Alembic 迁移
- [ ] Docker 部署

---

## 记录

（已有需求、已讨论过的想法）